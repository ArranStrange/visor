/**
 * Discussion Detail Page Default Plugins
 * 
 * This runtime file registers default Discussion detail page sections.
 * Plugins can override or extend these by using different priorities.
 */

import React from "react";
import {
  DiscussionDetailToolbar,
  DiscussionDetailSection,
} from "lib/slots/slot-definitions";

// Discussion detail slots are available for future plugin extensions
// Current implementation keeps existing functionality intact

