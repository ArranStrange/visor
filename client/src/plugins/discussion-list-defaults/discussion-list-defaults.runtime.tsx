/**
 * Discussion List Page Default Plugins
 * 
 * This runtime file registers default Discussion list page sections.
 * Plugins can override or extend these by using different priorities.
 */

import React from "react";
import {
  DiscussionFilters,
  DiscussionListHeader,
} from "lib/slots/slot-definitions";

// Discussion list slots are available for future plugin extensions
// Current implementation keeps existing functionality intact

