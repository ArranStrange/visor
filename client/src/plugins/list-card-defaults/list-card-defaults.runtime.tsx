/**
 * List Card Default Plugins
 * 
 * This runtime file registers default List card actions.
 * Plugins can override or extend these by using different priorities.
 */

import React from "react";
import {
  ListCardOverlay,
  ListCardFooter,
} from "lib/slots/slot-definitions";

// List card slots are available for future plugin extensions
// Current implementation keeps existing functionality intact

