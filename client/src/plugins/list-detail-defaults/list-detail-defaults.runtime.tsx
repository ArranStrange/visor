/**
 * List Detail Page Default Plugins
 * 
 * This runtime file registers default List detail page sections.
 * Plugins can override or extend these by using different priorities.
 */

import React from "react";
import {
  ListDetailHeader,
  ListDetailToolbar,
  ListDetailSection,
} from "lib/slots/slot-definitions";

// List detail slots are available for future plugin extensions
// Current implementation keeps existing functionality intact

