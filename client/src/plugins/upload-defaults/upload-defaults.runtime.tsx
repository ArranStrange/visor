/**
 * Upload Page Default Plugins
 * 
 * This runtime file registers default Upload page sections.
 * Plugins can override or extend these by using different priorities.
 */

import React from "react";
import {
  UploadToolbar,
  UploadFormSection,
} from "lib/slots/slot-definitions";

// Upload slots are available for future plugin extensions
// Current implementation keeps existing functionality intact

